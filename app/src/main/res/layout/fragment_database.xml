<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewmodel"
            type="com.example.viewmodel.ui.viewModels.DatabaseViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- TODO: 7. prerobit na databinding -->
        <EditText
                android:id="@+id/word_input"
                android:text="@={viewmodel.inputText}"
                android:layout_width="0dp" android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp" app:layout_constraintEnd_toStartOf="@id/word_btn"/>

        <Button
            android:id="@+id/word_btn"
            android:text="@string/save"
            android:onClick="@{() -> viewmodel.insertWord()}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/word_input"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/delete_slovo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewmodel.deleteSlovo()}"
            android:text="@string/vymaz"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/word_btn" />

        <Button
            android:id="@+id/update_slovo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="@{() -> viewmodel.updateSlovo()}"
            android:text="@string/uprav_slova"
            app:layout_constraintEnd_toStartOf="@id/delete_slovo"
            app:layout_constraintTop_toTopOf="@id/delete_slovo" />

        <TextView
            android:id="@+id/words_text"
            android:text="@{viewmodel.words_as_text}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="8dp"
            app:layout_constraintTop_toBottomOf="@+id/delete_slovo" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>